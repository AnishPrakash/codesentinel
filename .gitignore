.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
# The trained model is ~2 KB, not the multi-MB binary the original
# rule assumed, so it is committed rather than fetched from a release.
!models/triage.onnx
!models/feature_scaler.json
!models/.gitkeep
data/raw/
data/processed/
data/sources/
*.db
report.md

# vscode extension
vscode-extension/node_modules/
vscode-extension/out/
vscode-extension/*.vsix

# Per-developer interpreter path - see settings.json.example
vscode-extension/.vscode/settings.json

# Training corpora - large, and not ours to redistribute
juliet-java/
*juliet-test-suite*.zip
Data/CVEfixes.db
CVEfixes*.zip
_to_delete/
CVEfixes-1.0.8/
